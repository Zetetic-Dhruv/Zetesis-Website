# Stage 6 Review Bundle

## Contract

Review the Module 2 client package and PDF against `../platform/Module2_Design_Spec_v0.95.pdf`, `../platform/Module2_Wireframe_v0.95.pdf`, and the approved Stage 6 contract.

Attack recipient utility, content completeness, visual quality, correctness, provenance, artifact immutability, and lazy/adversarial use. A pass requires every findings array to be empty.

## Release Boundary

- Deliverable: **Bethany House Recommendation Brief**.
- It is a client-facing recommendation memo, not a classroom process report.
- Confidence remains omitted because the sealed label-quality gate did not pass.
- Stage 7 owns instructor presentation, but current instructor download endpoints must already return the same updated renderer.
- Module 1 reports and saved artifacts remain readable.

## Locked Source Of Record

The package compiler accepts only the server-stored locked state and preserves:

- the student-selected recommendation;
- the complete admitted candidate field;
- evidence against every candidate;
- failure tripwires for every candidate;
- the decision frame;
- loss bearer, accountability location, reversibility, and held constants.

The model supplies connective consultant prose only. It cannot change the selected bet, candidate set, contrary evidence, tripwires, or human judgments. Browser workspace saves cannot inject `currentPreview`, `sourceHash`, timestamps, or saved-version IDs.

The browser never receives the OpenAI credential. Preview and saved-version PDF bytes are generated by the Worker. Saved versions contain an immutable state snapshot, document JSON, plain text, and PDF artifact.

## Client Artifact Rules

- Use Bethany House-facing language.
- State a current decision position, not a probability of success.
- Include the complete structured document in one PDF.
- Exclude URT labels, internal prompts, model notes, confidence, student-performance language, and classroom narration.
- Use ASCII-safe visible symbols, clear hierarchy, generous section spacing, and page numbers.
- Do not draw horizontal rules.

## Browser Exercise

A fresh local account completed GROUND, BOARD, and LOCK, then:

1. Generated a server package and real PDF blob without exposing markdown.
2. Downloaded the current PDF.
3. Saved immutable Version 1 with one click.
4. Viewed Version 1 in the saved versions list.
5. Edited a locked judgment and observed the current preview become stale while Version 1 remained downloadable.

Observed browser facts:

- Mobile viewport: `390 x 844`.
- Document width: `390`; overflow element count: `0`.
- Embedded PDF width: `306`; visible inside the package panel.
- Login/register grid remains responsive after scoping the application grid rule.

## PDF Exercise

Generated artifact:

- `tmp/pdfs/module2-recommendation-smoke.pdf`
- SHA-256: `e1fdcc23fed331804f0264bf0543b2d1a3d31f4adcdea5a7e23860884faf6014`
- Three rendered pages: `tmp/pdfs/module2-recommendation-smoke-1.png` through `-3.png`.

Visual inspection found:

- Page 1 establishes the frame, recommendation, rationale, present decision position, and first candidate.
- Page 2 carries the remaining candidate field with evidence against and tripwires.
- Page 3 carries decision commitments, held constants, and the reopening condition.
- No orphaned commitments heading, horizontal rules, random symbols, clipped text, or markdown-like output.

The PDF smoke test extracts text and verifies that every client-facing string in the source document JSON appears in the single PDF. It also renders every page to PNG and checks nonblank pages, internal-language exclusions, confidence absence, prompt absence, and horizontal-rule absence.

The fixture begins with a fully populated locked state, not a pre-reduced document. Every admitted candidate contains origin, internal frame-basis trace IDs, distinctiveness, supporting evidence, contrary evidence, tripwires, and criterion evaluations. The compiler preserves all client-legible substance as `distinction`, `supportingEvidence`, `evidenceAgainst`, `tripwires`, and `decisionCriteria`. Internal record IDs and origin machinery are deliberately excluded from the client contract and tested absent. Each subsection heading is kept with at least its first item across page breaks.

## Deterministic Evidence

- `node --check` for `studio.js`, `module2-page.js`, `module2-package.js`, and `module2-pdf.js`: pass.
- Rendered Module 2 client script extraction and syntax check: pass.
- `npm run test:module2`: pass.
- `npm run test:offline`: pass.
- `npm run test:pdf`: pass.
- `git diff --check`: pass.
- Workspace package-forgery regression: pass.
- Locked-source stale-preview regression: pass.
- Immutable saved-version regression: pass.
- Student and instructor Module 2 PDF route regression: pass.
- Model-access block still permits saved PDF download: pass.

## Source Integrity

- Tracked Stage 6 diff SHA-256: `8454a6e12c06d568b66db0aebdfee86a307c357a3543c69e4736c058da662171`.
- `src/module2-package.js`: `53f1dc79e5b770788d5ca3c6b139d39926962770929c6c1568fca2795876ea2c`.
- `src/module2-pdf.js`: `fa1509264f57768123ddbb58c1a9a34facc00a340103cf924cb8a23cb4ff745d`.
- `test/module2-pdf.mjs`: `79dbfee3091b05158ae4f338ffd98a718f6f2dc0aacf2a1eef98491c6e3f0eb3`.
- `test/module2_pdf_smoke.py`: `e05fcb5ab4777d0f170afa71a0756b60047754e4135041f339f979336a2846e9`.

## Files In Scope

- `src/module2-package.js`
- `src/module2-pdf.js`
- `src/module2-page.js`
- `src/module2-state.js`
- Module 2 LLM/package/report handlers in `src/studio.js`
- `test/module2-pdf.mjs`
- `test/module2_pdf_smoke.py`
- `test/module2-baseline.mjs`
- `test/local-offline-smoke.mjs`
- `package.json`

## Required Response

Return JSON with:

- `verdict`
- `specCoverage`
- `utilityFindings`
- `qualityFindings`
- `correctnessFindings`
- `evidence`
- `requiredChanges`

Use `verdict: "pass"` only when every findings array and `requiredChanges` is empty.
